<h1>Localisation list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Commune</th>
            <th>Cp</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('localisation_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.commune }}</td>
            <td>{{ entity.cp }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('localisation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('localisation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('localisation_new') }}">
            Create a new entry
        </a>
    </li>
</ul>

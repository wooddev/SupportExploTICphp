<h1>Localisation</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Commune</th>
            <td>{{ entity.commune }}</td>
        </tr>
        <tr>
            <th>Cp</th>
            <td>{{ entity.cp }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('localisation') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('localisation_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('localisation_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>

{%extends "ExploticTiersBundle::layout.html.twig" %}

{% block body %} 
<h1>Stagiaire creation</h1>

<form action="{{ path('stagiaire_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('stagiaire') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock%}
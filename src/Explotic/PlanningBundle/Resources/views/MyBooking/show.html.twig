{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MyBooking</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Statutrdv</th>
                <td>{{ entity.statutRdv }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('mybooking') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}

<h1>CreneauPref</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>DisponibiliteTotale</th>
            <td>{{ entity.disponibiliteTotale }}</td>
        </tr>
        <tr>
            <th>transporteur</th>
            <td>{{ entity.transporteur.nom }}</td>
        </tr>
        <tr>
            <th>creneauModele</th>
            <td>{{ entity.creneaumodele }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('creneaupref') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('creneaupref_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('creneaupref_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>

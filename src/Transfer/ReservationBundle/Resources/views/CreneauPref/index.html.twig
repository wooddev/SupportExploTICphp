{%extends "::base.html.twig"%}
{%block body%}
<h1>CreneauPref list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>DisponibiliteTotale</th>
            <th>transporteur</th>
            <th>creneauModele</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('creneaupref_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.disponibiliteTotale }}</td>
            <td>{{ entity.transporteur.nom }}</td>
            <td>{{ entity.creneaumodele }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('creneaupref_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('creneaupref_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('creneaupref_new') }}">
            Create a new entry
        </a>
    </li>
    <li>
        <a href="{{ path('creneaupref_reset') }}">
            Create a new entry
        </a>
    </li>
</ul>
{%endblock%}
<h1>CreneauModele</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Jour</th>
            <td>{{ entity.jour }}</td>
        </tr>
        <tr>
            <th>Heure</th>
            <td>{{ entity.heure }}</td>
        </tr>
        <tr>
            <th>Minute</th>
            <td>{{ entity.minute }}</td>
        </tr>
        <tr>
            <th>Duree</th>
            <td>{{ entity.duree }}</td>
        </tr>
        <tr>
            <th>Heuredebut</th>
            <td>{{ entity.heureDebut }}</td>
        </tr>
        <tr>
            <th>Heurefin</th>
            <td>{{ entity.heureFin }}</td>
        </tr>
        <tr>
            <th>DisponibiliteTotale</th>
            <td>{{ entity.disponibiliteTotale }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('creneaumodele') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('creneaumodele_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('creneaumodele_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>

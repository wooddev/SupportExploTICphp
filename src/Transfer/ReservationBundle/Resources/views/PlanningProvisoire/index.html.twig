<h1>PlanningProvisoire list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Limite</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('planningprovisoire_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.limite }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('planningprovisoire_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('planningprovisoire_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('planningprovisoire_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
